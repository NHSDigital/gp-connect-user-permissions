type: object
description: |
  An object representing the level of permission a patient has to the
  appointments service along with the maximum level of access that
  could be requested.
required:
  - accessLevel
  - accessLevelMax
properties:
  accessLevel:
    $ref: "AppointmentsAccessLevelEnum.yaml"
  accessLevelMax:
    $ref: "AppointmentsAccessLevelMaxEnum.yaml"
