type: object
description: |
  Object containing the `accessLevel` to update the prescriptions service to.
required:
  - permissionType
  - accessLevel
properties:
  permissionType:
    $ref: "PrescriptionsPermissionTypeEnum.yaml"
  accessLevel:
    $ref: "PrescriptionsAccessLevelEnum.yaml"
