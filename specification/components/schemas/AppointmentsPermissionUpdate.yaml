type: object
description: |
  Object containing the `accessLevel` to update the appointments service to.
required:
  - permissionType
  - accessLevel
properties:
  permissionType:
    $ref: "AppointmentsPermissionTypeEnum.yaml"
  accessLevel:
    $ref: "AppointmentsAccessLevelEnum.yaml"
